---
"@kalada/core": minor
---

Add the `kalada-v1` canonical typed-function AST, factories, schemas, static contracts, lexical closures, bounded recursion, and iterative continuation runtime for Kalada #23 (K6-2) and #24 (K6-3), parent #6, and Formbar #93. Collection execution and module/import hooks remain excluded.
