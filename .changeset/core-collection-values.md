---
"@kalada/core": minor
---

Execute bounded `map`, `filter`, `some`, and `every` core-function values through the shared `kalada-v1` callable continuation machine for Kalada #25 (K6-4), parent #6, and Formbar #93. Modules and imports from #21 remain excluded.
