---
"@kalada/core": patch
---

Allow canonical ordered comparisons with an explicit number or string domain to compile dynamic operands, which are checked against that domain at runtime as documented.
