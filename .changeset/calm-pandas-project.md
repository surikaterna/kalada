---
"@kalada/projection": minor
---

Add deterministic compiled evaluation for projection `value`, `object`, `array`, and strict-boolean
`if` nodes, including contextual `Option.none` omission and transactional output limits.
