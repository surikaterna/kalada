---
"@kalada/syntax": minor
---

Add the first browser-safe Kalada v1 syntax package with lossless tokens and CST, bounded parsing and recovery, static source dispatch, canonical lowering and source maps, and an idempotent formatter. Function, call, constructor, match, import, module, and general ADT syntax remain intentionally excluded.
