---
"@kalada/projection": minor
---

Add bounded deterministic `map` evaluation with lexical item and index bindings, cumulative
iteration limits, omission compaction, and transactional output accounting.
