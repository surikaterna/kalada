# @kalada/projection

Canonical contracts, compilation, and deterministic evaluation for Kalada projection v1.

Compiled projections evaluate `value`, `object`, `array`, and `if` nodes through public
`@kalada/core/kalada-v1` programs and resolvers. `map` execution remains staged for the next
projection runtime release.
